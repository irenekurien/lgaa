import Head from 'next/head';

const Home = (): JSX.Element => {
    return (
        <h1 className="text-3xl font-bold underline">
          Hello world!
        </h1>
      )
    
};

export default Home;
